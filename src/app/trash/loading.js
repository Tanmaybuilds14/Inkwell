import { GlobalLoadingSkeleton } from "@/components/global-loading";

/** Instant skeleton for /trash — mirrors the trash page layout (header + list). */
export default function Loading() {
  return <GlobalLoadingSkeleton />;
}
