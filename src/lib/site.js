/**
 * Public links used across the landing page (navbar, hero buttons, feature
 * sections, footer). Defined once so the repository URL lives in exactly one
 * place — every surface that links out imports from here.
 */
export const GITHUB_URL = "https://github.com/Tanmaybuilds14/inkwell";

/** The License link in the footer points at the README's license section. */
export const LICENSE_URL = `${GITHUB_URL}#license`;
